part of 'front_library.dart';

class PectolarisMajor extends FrontMuscle {
  PectolarisMajor(super.size);

  @override
    get _left => leftPectolarisMajor;

  @override
  get _right => rightPectolarisMajor;
}
